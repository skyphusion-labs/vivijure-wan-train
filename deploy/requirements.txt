# Minimal runtime deps for the Wan train worker (vivijure conda env).
# GPU training runs in the isolated aitoolkit env; this env handles R2 I/O + job orchestration.
runpod>=1.12.0
boto3>=1.43.67
PyYAML>=6.0.3
huggingface_hub>=1.29.0
